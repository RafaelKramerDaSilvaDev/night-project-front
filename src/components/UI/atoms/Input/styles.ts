import styled from 'styled-components'

export const Input = styled.input`
  outline: none;

  border: none;

  width: 100%;

  color: #1b262c;
  font-size: 1rem;
  font-weight: 300;

  padding-bottom: 0.25rem;
`
